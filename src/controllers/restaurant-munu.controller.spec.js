import RestaurantMenuController from './restaurant-menu.controller';

describe('RestaurantMenuController', () => {
  // eslint-disable-next-line no-unused-vars
  let controller;

  beforeEach(() => {
    jest.clearAllMocks();
    controller = new RestaurantMenuController();
  });
});
